
/**
 * Roter Faden
 * - Java-Klasse
 * - main-Methode
 * - Einfache Datentypen (int,double,boolean,char,...) (Bit-Grenzen)
 * - Hexadezimal, Oktal, Binär (0x, 0, 0b)
 * - Referenzdatentypen (String,Array,Object) (equals)
 * - char -> int (ASCI-Tabelle)
 * - a = a++;
 * - if, else, switch, for, while, do-while, continue, break, return
 * - String-Operationen
 * - Arrays (auch mehrdimensional)
 * - Fehlerbehandlung in Java (Exceptions: geprüft, ungeprüft, try/catch/finally, throws, throw new)
 * - Assertions (nicht behandelt)
 * - Klassenmethoden
 * - Klassenvariablen
 * - Instanzerzeugung (new)
 * - Konstruktor(en)
 * - Instanzvariablen
 * - Instanzmethoden
 * - this
 * - toString
 * - Kapselung (private, get, set)
 * - Vererbung (extends, super)
 * - Abstrakte Klasse (abstract)
 * - Interface (implements (extends), default)
 * - Generics (<T>)
 * - Wildcards
 * - Sortierung (Insertion Sort, Selection Sort)
 * - Comparable
 * - Datenstrukturen (Collection, List, LinkedList, ArrayList, Stack, HashMap)
 */

import java.util.AbstractList;
import java.util.ArrayList;
import java.util.Collection;
import java.util.HashMap;
import java.util.LinkedList;
import java.util.List;
import java.util.Stack;
import java.util.Vector;

public class Testprogramm {

	@SuppressWarnings("unused")
	public static void main(String[] args) {
		
		Iterable<String> i;
		Collection<String> c;
		List<String> l;
		LinkedList<String> ll;
		AbstractList<String> abl;
		ArrayList<String> al;
		Vector<String> v;
		Stack<String> s;
		HashMap<String,String> hm;
		
	}
	
}
